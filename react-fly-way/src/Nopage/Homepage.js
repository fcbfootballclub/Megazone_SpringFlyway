import React from 'react'

const Homepage = (props) => {
  return (
    <div className='container center'>
        This is home page. Login to use all functions
    </div>
  );
};

export default Homepage;
